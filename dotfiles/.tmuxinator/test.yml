# ~/.tmuxinator/test.yml
# you can make as many tabs as you wish...

project_name: mainline code
project_root: .
socket_name: code # Not needed.  Remove to use default socket
# rvm: 1.9.2@rails_project
# pre: sudo /etc/rc.d/mysqld start
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain bash
        - top
  - shell: hg pull
  - logs: tail -f build.out
  - server: ssh chrisf@peppa
